<%= form_for @score_criterium, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :score_criteria, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :score_criteria, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                score_criteria_path, :class => 'btn' %>
  </div>
<% end %>
